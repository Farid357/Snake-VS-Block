using Snake.Model;
using Zenject;

namespace Snake.GameLogic
{
    //public sealed class BonusBlockPresenter : BlockPresenter
    //{
    //    private SnakeCircles _model;

    //    [Inject]
    //    public void Constructor(SnakeCircles model) => _model = model;

    //    protected override BlockHealth Get() => new BonusBlock(new SnakeImmortalAbility(_model), Health);
    //}
}
